# The demo app has no separate UAT deployment, so the URLs stay the same and only the
# run settings differ. Point BASE_URL/API_BASE_URL at a real UAT host when there is one.

HEADLESS=False

DEFAULT_TIMEOUT=30000

EXPECT_TIMEOUT=30000
